pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
plugins {
    id "com.capacitorjs.app.settings" version "8.3.0"
}

include ':app'
rootProject.name = 'Gevabal'
